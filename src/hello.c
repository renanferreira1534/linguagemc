// imporatando a biblioteca padrão de entrada e saida de dados
// std(standard) i(input) o(output)
#include <stdio.h>
// inicio do programa.
int main(){
// comando para exibir o testo hello na tela
// o comando printf só executa porque voce fez uma importação
// da biblioteca stdio.
        printf("hello world\n");
// o comando abaix informa ao compilador que programa acabou
        return 0;
 
}
